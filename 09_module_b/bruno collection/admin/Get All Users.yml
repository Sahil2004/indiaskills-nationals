info:
  name: Get All Users
  type: http
  seq: 9

http:
  method: GET
  url: "{{base_url}}/users?limit=2&cursor=eyJpZCI6NH0="
  params:
    - name: limit
      value: "2"
      type: query
    - name: cursor
      value: eyJpZCI6NH0=
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
