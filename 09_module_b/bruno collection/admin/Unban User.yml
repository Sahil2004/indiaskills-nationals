info:
  name: Unban User
  type: http
  seq: 10

http:
  method: PUT
  url: "{{base_url}}/users/1/unban"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
