info:
  name: Update Song Order
  type: http
  seq: 10

http:
  method: PUT
  url: "{{base_url}}/albums/2/songs/order"
  body:
    type: json
    data: |-
      {
        "song_ids":[12, 11]
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
