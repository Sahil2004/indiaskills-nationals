info:
  name: Get Album Cover
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/albums/2/cover"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
