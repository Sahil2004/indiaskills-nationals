info:
  name: Get Songs in Album
  type: http
  seq: 5

http:
  method: GET
  url: "{{base_url}}/albums/2/songs"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
